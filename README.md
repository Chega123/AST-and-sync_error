# AST-and-sync_error
falta lo de hacer q en el mensaje de los errores se indique la linea y columna, ademas de mejorar los mensajes y tmb tiene un problema q en las funciones por alguna razon cada que se crea una funcion esta copmo separada por un nodo vacio, no logro encontrar aun porque, para la parte de grafico debes instalar graphviz, creo q se puede instalar con brew si no me equivoco.

Tambien faltaria q en el scanner los tokens se guarden con el mensaje q leen tipo el token_num si lo q leyo era un 5 pues q se guarde en algun otro parametro de la estructura ese 5 para asi poder usar esos datos al imprimir el arbol en lugar de los tokens
